[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "anastasia-sdk"
version = "0.3.0"
description = "ANASTASiA — AI flight booking agent + OTA platform powered by Picasso Travel / AERTiCKET Redbox API"
readme = "README.md"
license = {text = "Proprietary"}
requires-python = ">=3.9"
authors = [
    {name = "MYSTES KYRIOS LLC"},
]
keywords = ["anastasia", "picasso", "redbox", "aerticket", "flights", "travel", "booking", "gds", "ota", "ai-agent"]

dependencies = [
    "requests>=2.28",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
auth = [
    "playwright>=1.40",
    "pyotp>=2.8",
]
agent = [
    "anthropic>=0.40",
    "flask>=3.0",
]
billing = [
    "stripe>=7.0",
]
all = [
    "anastasia-sdk[auth,agent,billing]",
]
dev = [
    "pytest>=7.0",
    "ruff>=0.1",
]

[project.scripts]
anastasia = "picasso.agent.assist_cli:main"

[tool.setuptools.packages.find]
include = ["picasso*"]
