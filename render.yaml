services:
  - type: web
    name: mystes-web
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: mystes-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: REDIS_URL
        fromService:
          name: mystes-redis
          type: redis
          property: connectionString
    healthCheckPath: /
    autoDeploy: true

  - type: redis
    name: mystes-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

databases:
  - name: mystes-db
    plan: free
    databaseName: mystes_db
    user: mystes
